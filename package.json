{
  "devDependencies": {
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "json-server": "^0.16.3"
  },
  "scripts" : {
    "start:db": "json-server -w db.json --port 3004",
    "start:parcel": "parcel index.html"
  }
}
